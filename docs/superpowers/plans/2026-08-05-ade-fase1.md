# ADE Fase 1: Backend LoopForge Extension — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) ou superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implementar a fundação de backend da ADE no LoopForge — checkpointer assíncrono + Trajectories API, cliente MCP via SDK oficial, provider LLM nativo com streaming, provider Ollama, `lf serve --no-ui` e config central `ade.yaml` com HITL timeout gracioso.

**Architecture:** Backend estendido dentro de `agentes/LoopForge/src/lf/` (E1 — um repo, uma versão). 1a migra o checkpointer de `SqliteSaver` síncrono para `AsyncSqliteSaver` em `.loopforge/trajectories.db` e expõe rotas REST de trajetórias; 1b adiciona um pacote `lf/mcp` sobre o SDK oficial `mcp` com allowlist deny-by-default; 1d adiciona `NativeLLMProvider` (HTTP streaming, fallback nativo→OpenCode→Mock) e 1c adiciona `OllamaProvider` com auto-descoberta; E9 centraliza config em `.loopforge/ade.yaml` (schema Pydantic + `GET/PATCH /api/v1/config`) e torna o timeout HITL gracioso (`decision_expired` + evento WS).

**Tech Stack:** Python >= 3.11, LangGraph (`AsyncSqliteSaver` de `langgraph.checkpoint.sqlite.aio`), aiosqlite (WAL), FastAPI (APIRouter), httpx (SSE streaming), SDK oficial `mcp` (novo — `uv add mcp`), Pydantic v2, pytest + pytest-asyncio.

## Global Constraints

- Todo código da Fase 1 entra em `agentes/LoopForge/src/lf/`; SPA em `web/loopforge-ade` é Fase 2 (fora de escopo).
- Testes: executar em `agentes/LoopForge` — `pytest tests/ --cov=src/lf --cov-fail-under=75`; 217 testes existentes devem seguir passando.
- WAL: `create_async_checkpointer` executa `PRAGMA journal_mode=WAL` explicitamente na abertura de `.loopforge/trajectories.db`, antes do `setup()`.
- Cache de streaming: `SQLiteLLMCache` armazena apenas o payload final consolidado do stream; deltas intermediários nunca são persistidos.
- HITL timeout: nó pausado que expira `hitl.timeout_seconds` → run transita para `decision_expired` + evento WS `human_decision_expired`; decisão tardia via `/decide` continua aceita; abort/resume funcionam.
- Sem prune do `trajectories.db` no V1 (E11).
- MCP: usar o SDK oficial `mcp` (D5); bridge LangGraph em `src/lf/mcp/` com permissões deny-by-default (allowlist).
- Identificadores de código em inglês; comentários e docs em português (AGENTS.md do repo).

---

### Task 1: Config central `AdeConfig` (E9 base)

**Files:**
- Modify: `agentes/LoopForge/src/lf/config/schema.py` (adicionar modelos ao final)
- Modify: `agentes/LoopForge/src/lf/config/loader.py` (adicionar funções)
- Test: `agentes/LoopForge/tests/test_ade_config.py` (Create)

**Interfaces:**
- Consumes: `load_config`/`save_config` existentes em `config/loader.py` (linhas 9/25) que já tratam YAML/JSON via `yaml.safe_load`/`json.loads` + `model_dump(mode="json")`.
- Produces (consumidos por Tasks 5, 6, 7, 8, 10):
  ```python
  class AdeMcpServer(BaseModel):      # name: str, command: str, args: list[str]=[], tools_allowlist: list[str]=[], enabled: bool=True
  class AdeBudget(BaseModel):         # max_usd: float = 10.0
  class AdeProviders(BaseModel):      # primary: str = "native", ollama_base_url: str = "http://localhost:11434"
  class AdeHITL(BaseModel):           # timeout_seconds: int = 300
  class AdeConfig(BaseModel):         # budget: AdeBudget=default, mcp_servers: list[AdeMcpServer]=[], providers: AdeProviders=default, hitl: AdeHITL=default
  def load_ade_config(config_path: str | Path = ".loopforge/ade.yaml") -> AdeConfig
  def save_ade_config(config: AdeConfig, config_path: str | Path = ".loopforge/ade.yaml") -> Path
  ```

- [ ] **Step 1: Write the failing test**

`agentes/LoopForge/tests/test_ade_config.py`:
```python
import os
from pathlib import Path
import pytest
from lf.config.loader import load_ade_config, save_ade_config
from lf.config.schema import AdeConfig, AdeMcpServer


def test_ade_config_defaults(tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    cfg = load_ade_config()  # arquivo não existe -> defaults
    assert cfg.budget.max_usd == 10.0
    assert cfg.providers.primary == "native"
    assert cfg.hitl.timeout_seconds == 300
    assert cfg.mcp_servers == []


def test_ade_config_roundtrip_yaml(tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    cfg = AdeConfig(mcp_servers=[AdeMcpServer(name="fs", command="npx", args=["-y", "@modelcontextprotocol/server-filesystem", "/tmp"])])
    path = save_ade_config(cfg, tmp_path / "ade.yaml")
    assert path.exists()
    loaded = load_ade_config(path)
    assert loaded.mcp_servers[0].name == "fs"
    assert loaded.mcp_servers[0].command == "npx"


def test_ade_config_invalid_raises(tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    (tmp_path / "ade.yaml").write_text("budget:\n  max_usd: 'nao-e-numero'\n")
    with pytest.raises(Exception):
        load_ade_config(tmp_path / "ade.yaml")
```

- [ ] **Step 2: Run test to verify it fails**

Run (em `agentes/LoopForge`): `pytest tests/test_ade_config.py -v`
Expected: FAIL com `ImportError: cannot import name 'load_ade_config'` (e `AdeConfig`).

- [ ] **Step 3: Write minimal implementation**

Adicionar ao final de `src/lf/config/schema.py`:
```python
class AdeMcpServer(BaseModel):
    name: str
    command: str
    args: list[str] = []
    tools_allowlist: list[str] = []
    enabled: bool = True


class AdeBudget(BaseModel):
    max_usd: float = 10.0


class AdeProviders(BaseModel):
    primary: str = "native"
    ollama_base_url: str = "http://localhost:11434"


class AdeHITL(BaseModel):
    timeout_seconds: int = 300


class AdeConfig(BaseModel):
    budget: AdeBudget = Field(default_factory=AdeBudget)
    mcp_servers: list[AdeMcpServer] = Field(default_factory=list)
    providers: AdeProviders = Field(default_factory=AdeProviders)
    hitl: AdeHITL = Field(default_factory=AdeHITL)
```

Adicionar ao final de `src/lf/config/loader.py`:
```python
def load_ade_config(config_path: str | Path = ".loopforge/ade.yaml") -> AdeConfig:
    path = Path(config_path)
    if not path.exists():
        return AdeConfig()
    if path.suffix in (".yaml", ".yml"):
        import yaml
        data = yaml.safe_load(path.read_text(encoding="utf-8")) or {}
    else:
        import json
        data = json.loads(path.read_text(encoding="utf-8"))
    return AdeConfig(**data)


def save_ade_config(config: AdeConfig, config_path: str | Path = ".loopforge/ade.yaml") -> Path:
    path = Path(config_path)
    path.parent.mkdir(parents=True, exist_ok=True)
    data = config.model_dump(mode="json")
    if path.suffix in (".yaml", ".yml"):
        import yaml
        path.write_text(yaml.dump(data, sort_keys=False), encoding="utf-8")
    else:
        import json
        path.write_text(json.dumps(data, indent=2), encoding="utf-8")
    return path
```
> Nota: `Field` já é importado em `schema.py` (usado nos modelos existentes). `save_config`/`load_config` existentes não são alterados.

- [ ] **Step 4: Run test to verify it passes**

Run (em `agentes/LoopForge`): `pytest tests/test_ade_config.py -v`
Expected: PASS (3 testes).

- [ ] **Step 5: Commit**

```bash
git add tests/test_ade_config.py src/lf/config/schema.py src/lf/config/loader.py
git commit -m "feat: adiciona AdeConfig central (.loopforge/ade.yaml)"
```

---

### Task 2: Factory de checkpointer assíncrono (1a)

**Files:**
- Create: `agentes/LoopForge/src/lf/pipeline/checkpointer.py`
- Test: `agentes/LoopForge/tests/test_async_checkpointer.py` (Create)

**Interfaces:**
- Consumes: `AsyncSqliteSaver` de `langgraph.checkpoint.sqlite.aio` (já em `langgraph-checkpoint-sqlite>=3.1.0`, lock 3.1.0); `SqliteSaver` de `langgraph.checkpoint.sqlite`; `aiosqlite>=0.19.0` (lock 0.22.1).
- Produces (consumido por Task 3):
  ```python
  def create_async_checkpointer(path: str | Path) -> AsyncSqliteSaver   # PRAGMA journal_mode=WAL explícito antes do setup()
  def create_sync_checkpointer(path: str | Path) -> SqliteSaver          # compat (caminho CLI legado)
  ```

- [ ] **Step 1: Write the failing test**

`agentes/LoopForge/tests/test_async_checkpointer.py`:
```python
import os
from pathlib import Path
import pytest
from lf.pipeline.checkpointer import create_async_checkpointer


def _wal_mode(db_path: Path) -> str:
    import sqlite3
    conn = sqlite3.connect(str(db_path))
    try:
        return conn.execute("PRAGMA journal_mode").fetchone()[0]
    finally:
        conn.close()


@pytest.mark.asyncio
async def test_async_checkpointer_opens_with_wal(tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    db = Path(".loopforge/trajectories.db")
    saver = create_async_checkpointer(db)
    assert db.exists()
    assert _wal_mode(db) == "wal"
    await saver.asetup()
    await saver.aclose()


@pytest.mark.asyncio
async def test_async_checkpointer_roundtrip_put_get(tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    saver = create_async_checkpointer(Path(".loopforge/trajectories.db"))
    await saver.asetup()
    config = {"configurable": {"thread_id": "proj-t1"}}
    checkpoint = {"v": 1, "ts": "2026-08-05T00:00:00Z", "channel_values": {"next_agent": "cpo"}}
    metadata = {"source": "loop", "step": 1}
    saved = await saver.aput(config, checkpoint, metadata, {})
    listed = await saver.alist(config, limit=10)
    assert any(c["thread_id"] == "proj-t1" for c in listed)
    await saver.aclose()
```

- [ ] **Step 2: Run test to verify it fails**

Run (em `agentes/LoopForge`): `pytest tests/test_async_checkpointer.py -v`
Expected: FAIL com `ModuleNotFoundError: No module named 'lf.pipeline.checkpointer'`.

- [ ] **Step 3: Write minimal implementation**

`src/lf/pipeline/checkpointer.py`:
```python
"""Factories de checkpointer LangGraph para a ADE.

Trajectories assíncronas em `.loopforge/trajectories.db` (modo WAL explícito
na abertura, antes do setup, para evitar travamentos de leitura/escrita
concorrentes durante chamadas da API).
"""
from pathlib import Path

from langgraph.checkpoint.sqlite import SqliteSaver
from langgraph.checkpoint.sqlite.aio import AsyncSqliteSaver

import aiosqlite


def create_async_checkpointer(path: str | Path) -> AsyncSqliteSaver:
    db_path = Path(path)
    db_path.parent.mkdir(parents=True, exist_ok=True)
    conn = aiosqlite.connect(str(db_path))
    # WAL explícito na abertura (antes do setup) — leituras/escritas concorrentes
    return AsyncSqliteSaver(conn=conn)


def create_sync_checkpointer(path: str | Path) -> SqliteSaver:
    import sqlite3
    db_path = Path(path)
    db_path.parent.mkdir(parents=True, exist_ok=True)
    conn = sqlite3.connect(str(db_path), check_same_thread=False)
    conn.execute("PRAGMA journal_mode=WAL")
    return SqliteSaver(conn=conn)
```

> **Atenção WAL (requisito do usuário):** `AsyncSqliteSaver(conn=aiosqlite.connect(...))` — o PRAGMA WAL precisa rodar na conexão **antes** de qualquer operação. Para garantir o WAL explícito, use um helper de conexão: troque a linha `conn = aiosqlite.connect(str(db_path))` por uma factory que executa o pragma:

```python
async def _open_conn(db_path: Path) -> aiosqlite.Connection:
    conn = await aiosqlite.connect(str(db_path))
    await conn.execute("PRAGMA journal_mode=WAL")
    await conn.execute("PRAGMA busy_timeout=5000")
    return conn


def create_async_checkpointer(path: str | Path) -> AsyncSqliteSaver:
    db_path = Path(path)
    db_path.parent.mkdir(parents=True, exist_ok=True)
    return AsyncSqliteSaver(conn_factory=lambda: _open_conn(db_path))
```
> (Se a versão do SDK aceitar `conn=` com conexão já aberta, o teste `_wal_mode == "wal"` valida o requisito de qualquer forma; ajuste ao que o teste exigir.)

- [ ] **Step 4: Run test to verify it passes**

Run (em `agentes/LoopForge`): `pytest tests/test_async_checkpointer.py -v`
Expected: PASS (WAL = "wal" no arquivo criado; round-trip `aput`/`alist`).

- [ ] **Step 5: Commit**

```bash
git add tests/test_async_checkpointer.py src/lf/pipeline/checkpointer.py
git commit -m "feat: factory create_async_checkpointer com WAL explicito (1a)"
```

---

### Task 3: Migração do dispatcher para trajectories.db (1a)

**Files:**
- Modify: `agentes/LoopForge/src/lf/orchestrator/task_dispatcher.py` (linhas 570-590 `list_checkpoints`, 588-648 `dispatch`, 654-702 `resume`)
- Modify: `agentes/LoopForge/src/lf/pipeline/graph.py` (linha 128-131 assinatura `build_graph`)
- Test: `agentes/LoopForge/tests/test_resume_checkpoint.py` (verificar compatibilidade) + `agentes/LoopForge/tests/test_trajectories_dispatch.py` (Create)

**Interfaces:**
- Consumes: `create_async_checkpointer`/`create_sync_checkpointer` (Task 2); `build_graph` (já aceita `checkpointer` — ampliar tipo para `Any | None`).
- Produces:
  - `dispatch()`/`resume()` mantêm assinaturas públicas síncronas, mas gravam em `.loopforge/trajectories.db` via async saver (`asyncio.run` no caminho síncrono).
  - `list_checkpoints() -> list[str]` agora lê `.loopforge/trajectories.db`.

- [ ] **Step 1: Write the failing test**

`agentes/LoopForge/tests/test_trajectories_dispatch.py`:
```python
from pathlib import Path
from lf.orchestrator.task_dispatcher import TaskDispatcher
from lf.config.schema import TaskSchema


def test_dispatch_persists_trajectories_db(tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    task = TaskSchema(id="t1", title="teste", agent_id="cpo", stack={"language": "python"})
    dispatcher = TaskDispatcher(mock_llm=True)
    result = dispatcher.dispatch(task)
    assert isinstance(result, dict)
    assert Path(".loopforge/trajectories.db").exists()
    assert "proj-t1" in dispatcher.list_checkpoints()


def test_resume_after_dispatch(tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    task = TaskSchema(id="t2", title="teste", agent_id="cpo", stack={"language": "python"})
    dispatcher = TaskDispatcher(mock_llm=True)
    dispatcher.dispatch(task)
    resumed = dispatcher.resume(project_id="proj", task_id="t2")
    assert isinstance(resumed, dict)
```

- [ ] **Step 2: Run test to verify it fails**

Run (em `agentes/LoopForge`): `pytest tests/test_trajectories_dispatch.py -v`
Expected: FAIL — `list_checkpoints()` lê `checkpoints.sqlite` e não encontra `proj-t1` (ou `trajectories.db` não existe).

- [ ] **Step 3: Write minimal implementation**

Em `src/lf/pipeline/graph.py`, ampliar a tipagem (linha 128):
```python
def build_graph(
    checkpointer: Any | None = None,          # InMemorySaver | SqliteSaver | AsyncSqliteSaver
    interrupt_after: list[str] | None = None,
    human_gate_enabled: bool = False,
):
```

Em `src/lf/orchestrator/task_dispatcher.py`, substituir a criação do checkpointer em `dispatch()` (linhas 590-598) e `resume()` (linhas 655-665) e `list_checkpoints()` (linhas 570-588):

```python
TRAJECTORIES_DB = Path(".loopforge/trajectories.db").resolve()


def _new_async_checkpointer():
    from lf.pipeline.checkpointer import create_async_checkpointer
    return create_async_checkpointer(TRAJECTORIES_DB)


async def _dispatch_async(self, initial_state, thread_id, task_id):
    from lf.pipeline.checkpointer import create_async_checkpointer
    checkpointer = create_async_checkpointer(TRAJECTORIES_DB)
    await checkpointer.asetup()
    graph = self._get_graph(checkpointer=checkpointer)
    config = {"configurable": {"thread_id": thread_id}}
    self._broadcast_ws("pipeline_started", task_id, {"idea": initial_state.get("title"), "node": initial_state.get("next_agent")})
    async for event in graph.astream(initial_state, config):
        for node_name, output in event.items():
            self._broadcast_ws("node_execution", task_id, {"node": node_name, "status": "completed", **self._safe_output(output)})
    if self.interactive:
        snapshot = await graph.aget_state(config)
        while snapshot.next:
            proceed = self._human_interrupt_handler(snapshot, config, graph)
            if not proceed:
                break
            async for event in graph.astream(None, config):
                for node_name, output in event.items():
                    self._broadcast_ws("node_execution", task_id, {"node": node_name, "status": "completed", **self._safe_output(output)})
            snapshot = await graph.aget_state(config)
    await checkpointer.aclose()
```

Em `dispatch()`:
```python
def dispatch(self, task: TaskSchema, project_id: str = "project", shared_state: dict | None = None) -> dict:
    initial_state = self._build_initial_state(task, project_id, shared_state=shared_state)
    thread_id = f"{project_id}-{task.id}"
    import asyncio
    asyncio.run(self._dispatch_async(initial_state, thread_id, task.id))
    return self.resume(project_id=project_id, task_id=task.id)
```

> Nota de implementação: se houver partes do grafo que usam `graph.stream` síncrono no HITL atual (`_human_interrupt_handler` recebe o app compilado — mantê-lo, pois é chamado com o grafo async; ajustar `graph.get_state` → `graph.aget_state` conforme o teste exigir). O objetivo do passo é: rodar o pipeline mock end-to-end via `astream` e persistir em `trajectories.db`. Simplifique o HITL interativo nesta migração apenas se os testes existentes de HITL continuarem passando; caso contrário, mantenha o caminho HITL síncrono via `_get_graph(checkpointer=create_sync_checkpointer(...))` (compat Task 2).

`resume()`:
```python
def resume(self, project_id: str = "project", task_id: str = "task-1") -> dict:
    if not TRAJECTORIES_DB.exists():
        raise RuntimeError(f"Nenhum banco de trajetórias encontrado em {TRAJECTORIES_DB}")
    import asyncio
    return asyncio.run(self._resume_async(project_id, task_id))
```

`list_checkpoints()`:
```python
def list_checkpoints(self) -> list[str]:
    if not TRAJECTORIES_DB.exists():
        return []
    import asyncio
    return asyncio.run(self._list_checkpoints_async())
```

- [ ] **Step 4: Run test to verify it passes + regressão**

Run (em `agentes/LoopForge`):
```
pytest tests/test_trajectories_dispatch.py tests/test_resume_checkpoint.py -v
```
Expected: PASS nos novos; `test_resume_checkpoint.py` segue verde (ou é ajustado apenas no caminho do banco).

- [ ] **Step 5: Commit**

```bash
git add tests/test_trajectories_dispatch.py src/lf/orchestrator/task_dispatcher.py src/lf/pipeline/graph.py
git commit -m "feat: dispatch/resume usam AsyncSqliteSaver em trajectories.db (1a)"
```

---

### Task 4: Trajectories API (1a)

**Files:**
- Create: `agentes/LoopForge/src/lf/api/trajectories.py`
- Modify: `agentes/LoopForge/src/lf/api/app.py` (incluir router em `create_app()`)
- Modify: `agentes/LoopForge/src/lf/api/schemas.py` (adicionar modelos de envelope)
- Test: `agentes/LoopForge/tests/test_api_trajectories.py` (Create)

**Interfaces:**
- Consumes: `TaskDispatcher.list_checkpoints()`, `create_async_checkpointer` (Task 2); `get_session` de `lf.api.database` (opcional); `ws_manager` de `lf.api.websocket_manager`.
- Produces (consumido por Fase 3):
  ```python
  class TrajectoryStep(BaseModel):   # node: str, ts: str, state_in: dict, state_out: dict, tokens: int=0, cost_usd: float=0.0, decision: str|None=None
  class TrajectoryEnvelope(BaseModel):  # schema_version: str="1.0", thread_id: str, created_at: str, steps: list[TrajectoryStep], events: list[dict]=[]
  trajectories_router = APIRouter(prefix="/api/v1/trajectories", tags=["Trajectories"])
  # GET  /{thread_id}/checkpoints
  # GET  /{thread_id}/checkpoints/{checkpoint_id}
  # GET  /{thread_id}/export
  # POST /import          (cria thread; 409 se thread existe; 422 schema_version inválido)
  # POST /{thread_id}/fork (deriva nova thread com sufixo -fork-<ts>)
  ```

- [ ] **Step 1: Write the failing test**

`agentes/LoopForge/tests/test_api_trajectories.py`:
```python
import os
from pathlib import Path
import pytest
from httpx import ASGITransport, AsyncClient
from lf.api.app import create_app


@pytest.mark.asyncio
async def test_trajectories_export_import_roundtrip(tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    app = create_app()
    transport = ASGITransport(app=app)
    async with AsyncClient(transport=transport, base_url="http://test") as ac:
        # seed: cria thread via checkpointer direto
        from lf.pipeline.checkpointer import create_async_checkpointer
        saver = create_async_checkpointer(Path(".loopforge/trajectories.db"))
        await saver.asetup()
        await saver.aput(
            {"configurable": {"thread_id": "proj-seed"}},
            {"v": 1, "ts": "2026-08-05T00:00:00Z", "channel_values": {"next_agent": "cpo"}},
            {"source": "loop", "step": 1}, {},
        )
        await saver.aclose()

        r = await ac.get("/api/v1/trajectories/proj-seed/export")
        assert r.status_code == 200
        data = r.json()
        assert data["schema_version"] == "1.0"
        assert data["thread_id"] == "proj-seed"

        # import em thread nova
        r2 = await ac.post("/api/v1/trajectories/import", json={**data, "thread_id": "proj-copy"})
        assert r2.status_code == 201
        assert r2.json()["thread_id"] == "proj-copy"


@pytest.mark.asyncio
async def test_trajectories_import_conflict_and_bad_schema(tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    app = create_app()
    transport = ASGITransport(app=app)
    async with AsyncClient(transport=transport, base_url="http://test") as ac:
        r = await ac.post("/api/v1/trajectories/import", json={
            "schema_version": "9.9", "thread_id": "x", "created_at": "2026-08-05T00:00:00Z",
            "steps": [], "events": [],
        })
        assert r.status_code == 422
```

- [ ] **Step 2: Run test to verify it fails**

Run (em `agentes/LoopForge`): `pytest tests/test_api_trajectories.py -v`
Expected: FAIL com `ModuleNotFoundError: lf.api.trajectories` (router não existe).

- [ ] **Step 3: Write minimal implementation**

`src/lf/api/trajectories.py`:
```python
"""Rotas de trajetórias (checkpoints + export/import/fork) da ADE."""
from datetime import datetime, timezone
from pathlib import Path
from typing import Any

from fastapi import APIRouter, HTTPException
from pydantic import BaseModel

from lf.pipeline.checkpointer import create_async_checkpointer

TRAJECTORIES_DB = Path(".loopforge/trajectories.db").resolve()


class TrajectoryStep(BaseModel):
    node: str
    ts: str
    state_in: dict[str, Any] = {}
    state_out: dict[str, Any] = {}
    tokens: int = 0
    cost_usd: float = 0.0
    decision: str | None = None


class TrajectoryEnvelope(BaseModel):
    schema_version: str = "1.0"
    thread_id: str
    created_at: str
    steps: list[TrajectoryStep] = []
    events: list[dict[str, Any]] = []


trajectories_router = APIRouter(prefix="/api/v1/trajectories", tags=["Trajectories"])


def _now() -> str:
    return datetime.now(timezone.utc).isoformat()


@trajectories_router.get("/{thread_id}/checkpoints")
async def list_checkpoints(thread_id: str):
    from lf.orchestrator.task_dispatcher import TaskDispatcher
    all_threads = TaskDispatcher().list_checkpoints()
    return [{"thread_id": t} for t in all_threads if t == thread_id]


@trajectories_router.get("/{thread_id}/checkpoints/{checkpoint_id}")
async def get_checkpoint(thread_id: str, checkpoint_id: str):
    saver = create_async_checkpointer(TRAJECTORIES_DB)
    await saver.asetup()
    try:
        value = await saver.aget_tuple(config={"configurable": {"thread_id": thread_id}}, checkpoint_id=checkpoint_id)
    finally:
        await saver.aclose()
    if value is None:
        raise HTTPException(status_code=404, detail=f"Checkpoint {checkpoint_id} não encontrado")
    return {"thread_id": thread_id, "checkpoint_id": checkpoint_id, "state": value.checkpoint.get("channel_values", {})}


@trajectories_router.get("/{thread_id}/export")
async def export_trajectory(thread_id: str):
    from lf.orchestrator.task_dispatcher import TaskDispatcher
    if thread_id not in TaskDispatcher().list_checkpoints():
        raise HTTPException(status_code=404, detail=f"Thread {thread_id} não encontrada")
    # Envelope mínimo — enriquecido com steps/events na Fase 3 (time-travel)
    return TrajectoryEnvelope(thread_id=thread_id, created_at=_now())


@trajectories_router.post("/import", status_code=201)
async def import_trajectory(envelope: TrajectoryEnvelope):
    if envelope.schema_version != "1.0":
        raise HTTPException(status_code=422, detail="schema_version suportada: 1.0")
    from lf.orchestrator.task_dispatcher import TaskDispatcher
    if envelope.thread_id in TaskDispatcher().list_checkpoints():
        raise HTTPException(status_code=409, detail="Thread já existe; sem merge no V1")
    # Persistir envelope como artefato da trajetória (steps completos chegam com o time-travel na Fase 3)
    import json
    meta = TRAJECTORIES_DB.with_name("trajectory-imports.json")
    records = []
    if meta.exists():
        records = json.loads(meta.read_text())
    records.append(envelope.model_dump(mode="json"))
    meta.write_text(json.dumps(records, indent=2))
    return envelope


@trajectories_router.post("/{thread_id}/fork", status_code=201)
async def fork_trajectory(thread_id: str):
    from lf.orchestrator.task_dispatcher import TaskDispatcher
    if thread_id not in TaskDispatcher().list_checkpoints():
        raise HTTPException(status_code=404, detail=f"Thread {thread_id} não encontrada")
    fork_id = f"{thread_id}-fork-{datetime.now(timezone.utc).strftime('%Y%m%dT%H%M%S')}"
    return {"fork_thread_id": fork_id, "source_thread_id": thread_id}
```

Em `src/lf/api/schemas.py` — não duplicar `TrajectoryEnvelope` (fica em `trajectories.py`); o router exporta o modelo próprio. Em `src/lf/api/app.py`, dentro de `create_app()` (após a definição das rotas existentes):
```python
from lf.api.trajectories import trajectories_router
app.include_router(trajectories_router)
```

- [ ] **Step 4: Run test to verify it passes**

Run (em `agentes/LoopForge`): `pytest tests/test_api_trajectories.py -v`
Expected: PASS (export 200 com `schema_version == "1.0"`; import 201 em thread nova; 422 em schema inválido; 409 quando thread existe).

- [ ] **Step 5: Commit**

```bash
git add tests/test_api_trajectories.py src/lf/api/trajectories.py src/lf/api/app.py
git commit -m "feat: rotas trajectories export/import/fork (1a)"
```

---

### Task 5: Pacote MCP — cliente, bridge, permissões, registry (1b)

**Files:**
- Run (uma vez): `uv add mcp` (em `agentes/LoopForge`)
- Create: `agentes/LoopForge/src/lf/mcp/__init__.py`, `agentes/LoopForge/src/lf/mcp/client.py`, `agentes/LoopForge/src/lf/mcp/bridge.py`, `agentes/LoopForge/src/lf/mcp/permissions.py`, `agentes/LoopForge/src/lf/mcp/registry.py`
- Test: `agentes/LoopForge/tests/test_mcp_client.py` (Create)

**Interfaces:**
- Consumes: SDK oficial `mcp` (`mcp.client.stdio.stdio_client`, `mcp import ClientSession`); `AdeConfig.mcp_servers` (Task 1).
- Produces (consumido por Task 6):
  ```python
  class MCPPermissionDenied(Exception): ...
  class MCPUnavailable(Exception): ...
  class MCPClient:  # connect()->None, disconnect()->None, list_tools()->list[dict], call_tool(name: str, args: dict)->Any
  class MCPRegistry:
      def __init__(self, config: AdeConfig): ...
      async def start_all(self) -> None
      async def stop_all(self) -> None
      async def list_servers(self) -> list[dict]           # [{name, status}]
      async def list_tools(self, server: str) -> list[dict]
      async def call_tool(self, server: str, name: str, args: dict) -> Any  # raise MCPPermissionDenied/MCPUnavailable
  ```

- [ ] **Step 1: Add dependency**

Run (em `agentes/LoopForge`): `uv add mcp`
Expected: `mcp` (e deps: `pydantic`, `anyio`, `httpx-sse` etc.) adicionado ao `pyproject.toml` e `uv.lock`.

- [ ] **Step 2: Write the failing test**

`agentes/LoopForge/tests/test_mcp_client.py`:
```python
import asyncio
import json
import sys
from pathlib import Path
import pytest
from lf.config.schema import AdeConfig, AdeMcpServer
from lf.mcp.permissions import MCPPermissionDenied
from lf.mcp.registry import MCPRegistry

FAKE_SERVER_SRC = r'''
import json, sys
from mcp.server import Server, stdio
server = Server("fake-tools")
@server.list_tools()
async def list_tools():
    return [{"name": "echo", "description": "echo input",
             "inputSchema": {"type": "object", "properties": {"text": {"type": "string"}}}}]
@server.call_tool()
async def call_tool(name: str, arguments: dict):
    return {"content": [{"type": "text", "text": json.dumps(arguments)}]}
async def main():
    async with stdio(server) as run:
        await run()
asyncio.run(main())
'''


@pytest.fixture
def fake_server_script(tmp_path) -> Path:
    p = tmp_path / "fake_server.py"
    p.write_text(FAKE_SERVER_SRC)
    return p


@pytest.mark.asyncio
async def test_registry_lists_and_calls_tool(fake_server_script, tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    cfg = AdeConfig(mcp_servers=[AdeMcpServer(name="fake", command=sys.executable,
                                              args=[str(fake_server_script)], tools_allowlist=["echo"])])
    registry = MCPRegistry(cfg)
    await registry.start_all()
    try:
        servers = await registry.list_servers()
        assert servers[0]["name"] == "fake"
        tools = await registry.list_tools("fake")
        assert [t["name"] for t in tools] == ["echo"]
        result = await registry.call_tool("fake", "echo", {"text": "oi"})
        assert "oi" in json.dumps(result)
    finally:
        await registry.stop_all()


@pytest.mark.asyncio
async def test_permission_denied_raises(fake_server_script, tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    cfg = AdeConfig(mcp_servers=[AdeMcpServer(name="fake", command=sys.executable,
                                              args=[str(fake_server_script)], tools_allowlist=["other"])])
    registry = MCPRegistry(cfg)
    await registry.start_all()
    try:
        with pytest.raises(MCPPermissionDenied):
            await registry.call_tool("fake", "echo", {"text": "x"})
    finally:
        await registry.stop_all()
```

- [ ] **Step 3: Run test to verify it fails**

Run (em `agentes/LoopForge`): `pytest tests/test_mcp_client.py -v`
Expected: FAIL com `ModuleNotFoundError: No module named 'lf.mcp'`.

- [ ] **Step 4: Write minimal implementation**

`src/lf/mcp/__init__.py`:
```python
"""Ponte MCP da ADE (SDK oficial, allowlist deny-by-default)."""
from lf.mcp.registry import MCPRegistry

__all__ = ["MCPRegistry"]
```

`src/lf/mcp/permissions.py`:
```python
class MCPPermissionDenied(Exception):
    """Tool não permitida pela allowlist do ade.yaml."""


class MCPUnavailable(Exception):
    """Servidor MCP não respondeu."""
```

`src/lf/mcp/client.py`:
```python
import asyncio
from typing import Any

from mcp import ClientSession, StdioServerParameters
from mcp.client.stdio import stdio_client

from lf.config.schema import AdeMcpServer


class MCPClient:
    """Cliente stdio por servidor MCP (SDK oficial)."""

    def __init__(self, spec: AdeMcpServer):
        self.spec = spec
        self._session: ClientSession | None = None
        self._ctx = None

    async def connect(self) -> None:
        params = StdioServerParameters(command=self.spec.command, args=self.spec.args)
        self._ctx = stdio_client(params)
        read, write = await self._ctx.__aenter__()
        self._session = await ClientSession(read, write).__aenter__()
        await self._session.initialize()

    async def disconnect(self) -> None:
        if self._session is not None:
            await self._session.__aexit__(None, None, None)
            self._session = None
        if self._ctx is not None:
            await self._ctx.__aexit__(None, None, None)
            self._ctx = None

    async def list_tools(self) -> list[dict]:
        if self._session is None:
            raise RuntimeError("MCPClient não conectado")
        resp = await self._session.list_tools()
        return [{"name": t.name, "description": t.description, "inputSchema": t.inputSchema} for t in resp.tools]

    async def call_tool(self, name: str, args: dict[str, Any]) -> Any:
        if self._session is None:
            raise RuntimeError("MCPClient não conectado")
        result = await self._session.call_tool(name, arguments=args)
        return result.model_dump()
```

`src/lf/mcp/bridge.py`:
```python
"""Conversão de schemas MCP (JSON Schema) em Pydantic Tool Definitions."""
from typing import Any

from pydantic import BaseModel, create_model


def json_schema_to_pydantic(name: str, schema: dict[str, Any]) -> type[BaseModel]:
    fields: dict[str, Any] = {}
    for prop, meta in (schema.get("properties") or {}).items():
        ptype = meta.get("type", "string")
        py_type = {"string": str, "integer": int, "number": float, "boolean": bool}.get(ptype, str)
        required = prop in (schema.get("required") or [])
        fields[prop] = (py_type if required else py_type | None, ... if required else None)
    return create_model(name, **fields)


def tools_to_langgraph(tools: list[dict]) -> list[dict]:
    """Retorna [{name, description, pydantic_model}] pronto para BaseTool da Fase 2."""
    out = []
    for t in tools:
        model = json_schema_to_pydantic(t["name"], t.get("inputSchema") or {})
        out.append({"name": t["name"], "description": t.get("description", ""), "pydantic_model": model})
    return out
```

`src/lf/mcp/registry.py`:
```python
import asyncio
from typing import Any

from lf.config.schema import AdeConfig
from lf.mcp.client import MCPClient
from lf.mcp.permissions import MCPPermissionDenied, MCPUnavailable


class MCPRegistry:
    """Dono dos clientes MCP ativos; start/stop por server declarado no ade.yaml."""

    def __init__(self, config: AdeConfig):
        self.config = config
        self._clients: dict[str, MCPClient] = {}
        self._status: dict[str, str] = {}

    async def start_all(self) -> None:
        for spec in self.config.mcp_servers:
            if not spec.enabled:
                continue
            client = MCPClient(spec)
            try:
                await client.connect()
                self._status[spec.name] = "connected"
            except Exception:
                self._status[spec.name] = "unavailable"
            self._clients[spec.name] = client

    async def stop_all(self) -> None:
        for client in self._clients.values():
            try:
                await client.disconnect()
            except Exception:
                pass
        self._clients.clear()

    def _server(self, name: str) -> MCPClient:
        if name not in self._clients:
            raise MCPUnavailable(f"Servidor MCP {name} não disponível")
        return self._clients[name]

    async def list_servers(self) -> list[dict]:
        return [{"name": s.name, "status": self._status.get(s.name, "stopped")} for s in self.config.mcp_servers]

    async def list_tools(self, server: str) -> list[dict]:
        if self._status.get(server) != "connected":
            raise MCPUnavailable(f"Servidor MCP {server} não conectado")
        return await self._server(server).list_tools()

    async def call_tool(self, server: str, name: str, args: dict[str, Any]) -> Any:
        spec = next((s for s in self.config.mcp_servers if s.name == server), None)
        if spec is None or name not in spec.tools_allowlist:
            raise MCPPermissionDenied(f"Tool {server}:{name} não permitida (allowlist do ade.yaml)")
        if self._status.get(server) != "connected":
            raise MCPUnavailable(f"Servidor MCP {server} não conectado")
        try:
            return await self._server(server).call_tool(name, args)
        except MCPPermissionDenied:
            raise
        except Exception as exc:
            self._status[server] = "unavailable"
            raise MCPUnavailable(f"Falha ao chamar {server}:{name}") from exc
```

- [ ] **Step 5: Run test to verify it passes**

Run (em `agentes/LoopForge`): `pytest tests/test_mcp_client.py -v`
Expected: PASS (servidor fake stdio lista e chama `echo`; `other` fora da allowlist → `MCPPermissionDenied`).

- [ ] **Step 6: Commit**

```bash
git add pyproject.toml uv.lock tests/test_mcp_client.py src/lf/mcp/
git commit -m "feat: cliente MCP via SDK oficial com allowlist deny-by-default (1b)"
```

---

### Task 6: Endpoints MCP (1b)

**Files:**
- Create: `agentes/LoopForge/src/lf/api/mcp.py`
- Modify: `agentes/LoopForge/src/lf/api/app.py` (incluir router; instanciar `MCPRegistry` no lifespan)
- Test: `agentes/LoopForge/tests/test_api_mcp.py` (Create)

**Interfaces:**
- Consumes: `MCPRegistry` (Task 5); `load_ade_config` (Task 1).
- Produces:
  ```python
  mcp_router = APIRouter(prefix="/api/v1/mcp", tags=["MCP"])
  # GET /servers -> list[{"name","status"}]
  # GET /servers/{name}/tools -> list[dict]
  ```

- [ ] **Step 1: Write the failing test**

`agentes/LoopForge/tests/test_api_mcp.py`:
```python
import sys
from pathlib import Path
import pytest
from httpx import ASGITransport, AsyncClient
from lf.api.app import create_app
from lf.config.loader import save_ade_config
from lf.config.schema import AdeConfig, AdeMcpServer

FAKE = r'''
import asyncio, json
from mcp.server import Server, stdio
server = Server("t")
@server.list_tools()
async def lt(): return [{"name": "ping", "description": "p", "inputSchema": {"type": "object"}}]
@server.call_tool()
async def ct(name, arguments): return {"content": [{"type": "text", "text": "pong"}]}
async def main():
    async with stdio(server) as run: await run()
asyncio.run(main())
'''


@pytest.mark.asyncio
async def test_mcp_api_lists_servers_and_tools(tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    fake = tmp_path / "fake.py"
    fake.write_text(FAKE)
    save_ade_config(AdeConfig(mcp_servers=[AdeMcpServer(name="t", command=sys.executable,
                                                        args=[str(fake)], tools_allowlist=["ping"])]),
                    tmp_path / ".loopforge" / "ade.yaml")
    app = create_app()
    transport = ASGITransport(app=app)
    async with AsyncClient(transport=transport, base_url="http://test") as ac:
        r = await ac.get("/api/v1/mcp/servers")
        assert r.status_code == 200
        assert any(s["name"] == "t" for s in r.json())
        r2 = await ac.get("/api/v1/mcp/servers/t/tools")
        assert r2.status_code == 200
        assert [t["name"] for t in r2.json()] == ["ping"]
```

- [ ] **Step 2: Run test to verify it fails**

Run (em `agentes/LoopForge`): `pytest tests/test_api_mcp.py -v`
Expected: FAIL com `ModuleNotFoundError: lf.api.mcp`.

- [ ] **Step 3: Write minimal implementation**

`src/lf/api/mcp.py`:
```python
"""Endpoints MCP da ADE (lista de servidores e ferramentas)."""
from fastapi import APIRouter, HTTPException

from lf.config.loader import load_ade_config
from lf.mcp.permissions import MCPUnavailable
from lf.mcp.registry import MCPRegistry

mcp_router = APIRouter(prefix="/api/v1/mcp", tags=["MCP"])


def _registry() -> MCPRegistry:
    return MCPRegistry(load_ade_config())


@mcp_router.get("/servers")
async def list_servers():
    reg = _registry()
    try:
        await reg.start_all()
        return await reg.list_servers()
    finally:
        await reg.stop_all()


@mcp_router.get("/servers/{name}/tools")
async def list_server_tools(name: str):
    reg = _registry()
    try:
        await reg.start_all()
        try:
            return await reg.list_tools(name)
        except MCPUnavailable as exc:
            raise HTTPException(status_code=503, detail=str(exc)) from exc
    finally:
        await reg.stop_all()
```

Em `src/lf/api/app.py`, dentro de `create_app()`:
```python
from lf.api.mcp import mcp_router
app.include_router(mcp_router)
```

- [ ] **Step 4: Run test to verify it passes**

Run (em `agentes/LoopForge`): `pytest tests/test_api_mcp.py -v`
Expected: PASS (servidores listados do `ade.yaml`; tools do server `t` = `["ping"]`).

- [ ] **Step 5: Commit**

```bash
git add tests/test_api_mcp.py src/lf/api/mcp.py src/lf/api/app.py
git commit -m "feat: endpoints GET /api/v1/mcp/servers e /tools (1b)"
```

---

### Task 7: `NativeLLMProvider` com streaming (1d)

**Files:**
- Modify: `agentes/LoopForge/src/lf/pipeline/llm_factory.py` (adicionar `NativeLLMProvider` + `stream()`; registrar)
- Modify: `agentes/LoopForge/src/lf/pipeline/cache.py` (garantir API `get/set` usada só com payload final — já é o caso)
- Test: `agentes/LoopForge/tests/test_native_llm_provider.py` (Create)

**Interfaces:**
- Consumes: `BaseLLMProvider` (contrato: `provider_name`, `generate(...)`, linha 234); `call_openrouter_api` (linha 24, retorna `(text, usage)`); `CostTracker.track(...)` (linha ~307); `SQLiteLLMCache.get/set` (cache.py:51/59); `LLMProviderRegistry.register` (linha 340).
- Produces (consumido pela UI Fase 2 e por Tasks 8/10):
  ```python
  class NativeLLMProvider(BaseLLMProvider):
      provider_name = "native"
      def generate(self, system_prompt, user_prompt, model=DEFAULT_OPENROUTER_MODEL, temperature=0.2,
                   schema_model=None, mock=False, cache=True, circuit_breaker=None) -> Any
      async def stream(self, system_prompt, user_prompt, model=DEFAULT_OPENROUTER_MODEL,
                       temperature=0.2, circuit_breaker=None) -> AsyncIterator[str]   # SSE: data: {...delta...}, [DONE]
  ```

- [ ] **Step 1: Write the failing test**

`agentes/LoopForge/tests/test_native_llm_provider.py`:
```python
import asyncio
import pytest
from httpx import MockTransport, Response
from lf.pipeline.llm_factory import NativeLLMProvider, LLMProviderRegistry


def _sse_body() -> str:
    return (
        'data: {"choices":[{"delta":{"content":"Ola"}}]}\n\n'
        'data: {"choices":[{"delta":{"content":" mundo"}}]}\n\n'
        "data: [DONE]\n\n"
    )


@pytest.fixture
def native():
    return NativeLLMProvider(api_key="test-key")


def test_native_generate_parses_text(native, monkeypatch):
    captured = {}

    def fake_request(url, **kwargs):
        captured["json"] = kwargs.get("json") or {}
        return Response(200, text=_sse_body())

    monkeypatch.setattr(native._client, "post", fake_request)
    text = native.generate("sys", "user", model="auto/best-free")
    assert text == "Ola mundo"
    assert captured["json"]["stream"] is False


@pytest.mark.asyncio
async def test_native_stream_emits_tokens_in_order(native, monkeypatch):
    async def fake_stream(*args, **kwargs):
        for chunk in _sse_body().split("\n\n"):
            yield Response(200, text=chunk)

    monkeypatch.setattr(native._client, "stream", fake_stream)
    tokens = [tok async for tok in native.stream("sys", "user")]
    assert tokens == ["Ola", " mundo"]


def test_native_registered_in_registry():
    provider = LLMProviderRegistry.get("native")
    assert provider.provider_name == "native"
```

- [ ] **Step 2: Run test to verify it fails**

Run (em `agentes/LoopForge`): `pytest tests/test_native_llm_provider.py -v`
Expected: FAIL com `ImportError: cannot import name 'NativeLLMProvider'`.

- [ ] **Step 3: Write minimal implementation**

Adicionar a `src/lf/pipeline/llm_factory.py`:
```python
class NativeLLMProvider(BaseLLMProvider):
    """Provider LLM nativo via HTTP (OpenRouter/Zen), streaming token a token.

    Cadeia de fallback: nativo -> OpenCode CLI -> Mock. O cache SQLiteLLMCache
    guarda apenas o payload final consolidado; deltas intermediários nunca são
    persistidos (requisito de streaming).
    """

    @property
    def provider_name(self) -> str:
        return "native"

    def __init__(self, api_key: str | None = None, base_url: str | None = None, timeout: float = 60.0):
        import httpx
        self.api_key = api_key or os.environ.get("OPENROUTER_API_KEY")
        self.base_url = base_url or DEFAULT_OPENROUTER_BASE_URL
        self.timeout = timeout
        self._client = httpx.Client(timeout=self.timeout)
        self._async_client = httpx.AsyncClient(timeout=self.timeout)

    def _headers(self) -> dict[str, str]:
        return {"Authorization": f"Bearer {self.api_key}", "Content-Type": "application/json"}

    def _parse_sse(self, body: str) -> str:
        text = []
        for line in body.splitlines():
            if not line.startswith("data:"):
                continue
            payload = line[5:].strip()
            if payload == "[DONE]":
                break
            import json as _json
            try:
                delta = _json.loads(payload).get("choices", [{}])[0].get("delta", {}).get("content", "")
            except Exception:
                delta = ""
            text.append(delta)
        return "".join(text)

    def generate(self, system_prompt, user_prompt, model=DEFAULT_OPENROUTER_MODEL,
                 temperature=0.2, schema_model=None, mock=False, cache=True, circuit_breaker=None) -> Any:
        from lf.pipeline.cache import SQLiteLLMCache
        if mock:
            return MockLLMProvider().generate(system_prompt, user_prompt, model=model,
                                              schema_model=schema_model, mock=True)
        if circuit_breaker is not None and not circuit_breaker.can_proceed():
            return self._fallback_generate(system_prompt, user_prompt, model, schema_model, mock, cache, circuit_breaker)
        full_prompt = f"{system_prompt}\n{user_prompt}"
        llm_cache = SQLiteLLMCache()
        if cache:
            hit = llm_cache.get(full_prompt)
            if hit is not None:
                return hit
        payload = {
            "model": model,
            "messages": [{"role": "system", "content": system_prompt}, {"role": "user", "content": user_prompt}],
            "temperature": temperature,
            "stream": False,
        }
        try:
            resp = self._client.post(f"{self.base_url}/chat/completions", json=payload, headers=self._headers())
            resp.raise_for_status()
            text = self._parse_sse(resp.text)
        except Exception:
            return self._fallback_generate(system_prompt, user_prompt, model, schema_model, mock, cache, circuit_breaker)
        # Payload final consolidado -> cache (requisito: nunca deltas)
        if cache:
            llm_cache.set(full_prompt, text)
        try:
            from lf.pipeline.llm_factory import CostTracker
            CostTracker().track(model, full_prompt, text)
        except Exception:
            pass
        return text

    def _fallback_generate(self, system_prompt, user_prompt, model, schema_model, mock, cache, circuit_breaker):
        try:
            return OpenCodeCLIProvider().generate(system_prompt, user_prompt, model=model,
                                                  schema_model=schema_model, mock=mock,
                                                  cache=cache, circuit_breaker=circuit_breaker)
        except Exception:
            return MockLLMProvider().generate(system_prompt, user_prompt, model=model,
                                              schema_model=schema_model, mock=True)

    async def stream(self, system_prompt, user_prompt, model=DEFAULT_OPENROUTER_MODEL,
                     temperature=0.2, circuit_breaker=None) -> "AsyncIterator[str]":
        payload = {
            "model": model,
            "messages": [{"role": "system", "content": system_prompt}, {"role": "user", "content": user_prompt}],
            "temperature": temperature,
            "stream": True,
        }
        async with self._async_client.stream("POST", f"{self.base_url}/chat/completions",
                                             json=payload, headers=self._headers()) as resp:
            resp.raise_for_status()
            async for line in resp.aiter_lines():
                if not line.startswith("data:"):
                    continue
                chunk = line[5:].strip()
                if chunk == "[DONE]":
                    break
                import json as _json
                try:
                    delta = _json.loads(chunk).get("choices", [{}])[0].get("delta", {}).get("content", "")
                except Exception:
                    delta = ""
                if delta:
                    yield delta
```

Registrar (após as 3 linhas existentes de `register`):
```python
LLMProviderRegistry.register(NativeLLMProvider())
```
> Nota: `DEFAULT_OPENROUTER_BASE_URL` e `DEFAULT_OPENROUTER_MODEL` já existem no módulo; usar os nomes reais presentes no arquivo (ex.: a base URL usada em `call_openrouter_api`). Ajuste as constantes conforme o código real.

- [ ] **Step 4: Run test to verify it passes**

Run (em `agentes/LoopForge`): `pytest tests/test_native_llm_provider.py -v`
Expected: PASS (parse SSE concatena "Ola mundo"; stream emite deltas em ordem; registry.get("native") resolve).

- [ ] **Step 5: Commit**

```bash
git add tests/test_native_llm_provider.py src/lf/pipeline/llm_factory.py
git commit -m "feat: NativeLLMProvider com streaming e fallback nativo->OpenCode->Mock (1d)"
```

---

### Task 8: `OllamaProvider` + auto-descoberta (1c)

**Files:**
- Create: `agentes/LoopForge/src/lf/pipeline/providers/ollama.py` (+ `src/lf/pipeline/providers/__init__.py` se não existir)
- Modify: `agentes/LoopForge/src/lf/pipeline/llm_factory.py` (registrar provider)
- Create: `agentes/LoopForge/src/lf/api/providers.py`
- Modify: `agentes/LoopForge/src/lf/api/app.py` (incluir router)
- Test: `agentes/LoopForge/tests/test_ollama_provider.py` (Create)

**Interfaces:**
- Consumes: `AdeConfig.providers.ollama_base_url` (Task 1) ou env `OLLAMA_HOST`; `httpx`.
- Produces:
  ```python
  class OllamaProvider:
      provider_name = "ollama"
      def __init__(self, base_url: str | None = None) -> None            # default http://localhost:11434
      def discover_models(self) -> list[str]                              # GET /api/tags -> ["model": {name}]
      def chat(self, messages: list[dict], model: str, stream: bool = False) -> str
      async def stream(self, messages: list[dict], model: str) -> AsyncIterator[str]
  # GET /api/v1/providers/ollama/models -> list[str]
  ```

- [ ] **Step 1: Write the failing test**

`agentes/LoopForge/tests/test_ollama_provider.py`:
```python
import pytest
from httpx import MockTransport, Response
from lf.pipeline.providers.ollama import OllamaProvider


def _tags_body() -> str:
    return '{"models": [{"name": "llama3.2"}, {"name": "qwen2.5"}]}'


def test_discover_models_parses_tags():
    provider = OllamaProvider(base_url="http://ollama:11434")

    def fake(request):
        assert request.url.path == "/api/tags"
        return Response(200, text=_tags_body())

    provider._client = provider._client.__class__(transport=MockTransport(fake))
    assert provider.discover_models() == ["llama3.2", "qwen2.5"]


@pytest.mark.asyncio
async def test_ollama_stream_emits_tokens():
    provider = OllamaProvider(base_url="http://ollama:11434")

    async def fake_stream(request):
        yield Response(200, text='{"message":{"content":"a"}}\n')
        yield Response(200, text='{"message":{"content":"b"}}\n')

    provider._async_client = provider._async_client.__class__(transport=MockTransport(fake_stream))
    tokens = [t async for t in provider.stream([{"role": "user", "content": "hi"}], model="llama3.2")]
    assert tokens == ["a", "b"]
```

- [ ] **Step 2: Run test to verify it fails**

Run (em `agentes/LoopForge`): `pytest tests/test_ollama_provider.py -v`
Expected: FAIL com `ModuleNotFoundError: lf.pipeline.providers.ollama`.

- [ ] **Step 3: Write minimal implementation**

`src/lf/pipeline/providers/ollama.py`:
```python
"""Provider Ollama (LLMs locais) com auto-descoberta via /api/tags."""
import json
import os
from typing import AsyncIterator

import httpx


class OllamaProvider:
    provider_name = "ollama"

    def __init__(self, base_url: str | None = None):
        self.base_url = (base_url or os.environ.get("OLLAMA_HOST") or "http://localhost:11434").rstrip("/")
        self._client = httpx.Client(timeout=60.0)
        self._async_client = httpx.AsyncClient(timeout=60.0)

    def discover_models(self) -> list[str]:
        resp = self._client.get(f"{self.base_url}/api/tags")
        resp.raise_for_status()
        return [m["name"] for m in resp.json().get("models", [])]

    def chat(self, messages: list[dict], model: str, stream: bool = False) -> str:
        resp = self._client.post(f"{self.base_url}/api/chat", json={"model": model, "messages": messages, "stream": stream})
        resp.raise_for_status()
        return resp.json().get("message", {}).get("content", "")

    async def stream(self, messages: list[dict], model: str) -> AsyncIterator[str]:
        async with self._async_client.stream(
            "POST", f"{self.base_url}/api/chat",
            json={"model": model, "messages": messages, "stream": True},
        ) as resp:
            resp.raise_for_status()
            async for line in resp.aiter_lines():
                if not line.strip():
                    continue
                try:
                    content = json.loads(line).get("message", {}).get("content", "")
                except json.JSONDecodeError:
                    content = ""
                if content:
                    yield content
```

Em `src/lf/pipeline/llm_factory.py`, registrar (importar e chamar):
```python
from lf.pipeline.providers.ollama import OllamaProvider
LLMProviderRegistry.register(OllamaProvider())
```

`src/lf/api/providers.py`:
```python
"""Endpoints de provedores LLM (auto-descoberta)."""
from fastapi import APIRouter, HTTPException

from lf.config.loader import load_ade_config
from lf.pipeline.providers.ollama import OllamaProvider

providers_router = APIRouter(prefix="/api/v1/providers", tags=["Providers"])


@providers_router.get("/ollama/models")
async def ollama_models():
    base_url = load_ade_config().providers.ollama_base_url
    try:
        return OllamaProvider(base_url=base_url).discover_models()
    except Exception as exc:
        raise HTTPException(status_code=503, detail=f"Ollama indisponível em {base_url}") from exc
```

Em `src/lf/api/app.py`:
```python
from lf.api.providers import providers_router
app.include_router(providers_router)
```

- [ ] **Step 4: Run test to verify it passes**

Run (em `agentes/LoopForge`): `pytest tests/test_ollama_provider.py -v`
Expected: PASS (discover parseia `/api/tags`; stream emite tokens "a","b" em ordem).

- [ ] **Step 5: Commit**

```bash
git add tests/test_ollama_provider.py src/lf/pipeline/providers/ src/lf/api/providers.py src/lf/api/app.py src/lf/pipeline/llm_factory.py
git commit -m "feat: OllamaProvider com auto-descoberta /api/tags (1c)"
```

---

### Task 9: `lf serve --no-ui` (1c)

**Files:**
- Modify: `agentes/LoopForge/src/lf/cli/commands/serve.py`
- Modify: `agentes/LoopForge/src/lf/api/app.py` (rotas de UI condicionais)
- Test: `agentes/LoopForge/tests/test_serve_no_ui.py` (Create)

**Interfaces:**
- Consumes: `create_app()` (atual, rotas `/` e `/dashboard` em app.py:79-83).
- Produces:
  ```python
  def create_app(ui_enabled: bool | None = None) -> FastAPI   # None -> lê env LF_UI_ENABLED != "0"
  # serve_cmd ganha: --no-ui (is_flag)
  ```

- [ ] **Step 1: Write the failing test**

`agentes/LoopForge/tests/test_serve_no_ui.py`:
```python
import pytest
from httpx import ASGITransport, AsyncClient
from lf.api.app import create_app


@pytest.mark.asyncio
async def test_dashboard_route_disabled_with_no_ui():
    app = create_app(ui_enabled=False)
    transport = ASGITransport(app=app)
    async with AsyncClient(transport=transport, base_url="http://test") as ac:
        r = await ac.get("/dashboard")
        assert r.status_code == 404
        h = await ac.get("/health")
        assert h.status_code == 200


@pytest.mark.asyncio
async def test_dashboard_route_present_by_default():
    app = create_app()
    transport = ASGITransport(app=app)
    async with AsyncClient(transport=transport, base_url="http://test") as ac:
        r = await ac.get("/dashboard")
        assert r.status_code == 200
```

- [ ] **Step 2: Run test to verify it fails**

Run (em `agentes/LoopForge`): `pytest tests/test_serve_no_ui.py -v`
Expected: FAIL com `TypeError: create_app() got an unexpected keyword argument 'ui_enabled'`.

- [ ] **Step 3: Write minimal implementation**

Em `src/lf/api/app.py`, alterar a assinatura (linha 50) e condicionar as rotas de UI (linhas 79-83):
```python
def create_app(ui_enabled: bool | None = None) -> FastAPI:
    settings = get_api_settings()
    if ui_enabled is None:
        ui_enabled = os.environ.get("LF_UI_ENABLED", "1") != "0"
    app = FastAPI(...)
    ...
    @app.get("/health")
    async def health(): ...

    if ui_enabled:
        @app.get("/")
        async def root(): ...
        @app.get("/dashboard")
        async def dashboard(): ...
    ...
    app.include_router(trajectories_router)
    app.include_router(mcp_router)
    app.include_router(providers_router)
    return app
```

Em `src/lf/cli/commands/serve.py`:
```python
@click.command(name="serve")
@click.option("--host", default="127.0.0.1", help="Endereço de host (padrão: 127.0.0.1)")
@click.option("--port", default=8000, type=int, help="Porta HTTP (padrão: 8000)")
@click.option("--reload", is_flag=True, help="Ativa modo auto-reload para desenvolvimento")
@click.option("--no-ui", is_flag=True, help="Não serve dashboard/SPA, apenas a API")
def serve_cmd(host: str, port: int, reload: bool, no_ui: bool):
    """Inicia o servidor de API REST, WebSockets e Web Dashboard do LoopForge v6."""
    api_key = os.getenv("LF_API_API_KEY") or os.getenv("LF_API_KEY")
    if not api_key:
        api_key = secrets.token_hex(16)
        os.environ["LF_API_API_KEY"] = api_key
    console.print(f"[bold cyan]⚡ Iniciando LoopForge Server em http://{host}:{port}[/bold cyan]")
    if not no_ui:
        console.print(f"[dim]   • Web Dashboard UI: http://{host}:{port}/dashboard[/dim]")
    console.print(f"[dim]   • API Documentation: http://{host}:{port}/docs[/dim]")
    console.print(f"[dim]   • WebSocket Streaming: ws://{host}:{port}/ws/streaming[/dim]")
    console.print(f"[bold green]🔐 Autenticação Ativada. X-API-Key:[/bold green] [bold white]{api_key}[/bold white]\n")
    if no_ui:
        os.environ["LF_UI_ENABLED"] = "0"
    uvicorn.run("lf.api.app:create_app", factory=True, host=host, port=port, reload=reload)
```

- [ ] **Step 4: Run test to verify it passes**

Run (em `agentes/LoopForge`): `pytest tests/test_serve_no_ui.py -v`
Expected: PASS (`/dashboard` 404 com `ui_enabled=False`; `/health` 200; `/dashboard` 200 por padrão).

- [ ] **Step 5: Commit**

```bash
git add tests/test_serve_no_ui.py src/lf/cli/commands/serve.py src/lf/api/app.py
git commit -m "feat: lf serve --no-ui serve apenas API (1c)"
```

---

### Task 10: Config API + HITL timeout gracioso (E9 + F1-13)

**Files:**
- Create: `agentes/LoopForge/src/lf/api/config.py`
- Modify: `agentes/LoopForge/src/lf/api/app.py` (incluir router)
- Modify: `agentes/LoopForge/src/lf/orchestrator/task_dispatcher.py` (usar `AdeConfig.hitl.timeout_seconds`; expirar → `decision_expired` + evento WS `human_decision_expired`)
- Test: `agentes/LoopForge/tests/test_config_api.py` + `agentes/LoopForge/tests/test_hitl_timeout.py` (Create)

**Interfaces:**
- Consumes: `AdeConfig` + `load_ade_config`/`save_ade_config` (Task 1); `ws_manager.broadcast` (eventos WS já usados: `human_decision_submitted`, etc.); `TaskDispatcher(hitl_timeout_seconds=...)` (parâmetro já existe, default 300).
- Produces:
  ```python
  config_router = APIRouter(prefix="/api/v1/config", tags=["Config"])
  # GET  / -> AdeConfig (model_dump)
  # PATCH / -> AdeConfig (valida antes de escrever; atomic write; 422 se inválido)
  ```

- [ ] **Step 1: Write the failing tests**

`agentes/LoopForge/tests/test_config_api.py`:
```python
import pytest
from httpx import ASGITransport, AsyncClient
from lf.api.app import create_app
from lf.config.loader import load_ade_config


@pytest.mark.asyncio
async def test_config_get_returns_defaults(tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    app = create_app()
    async with AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as ac:
        r = await ac.get("/api/v1/config")
        assert r.status_code == 200
        assert r.json()["hitl"]["timeout_seconds"] == 300


@pytest.mark.asyncio
async def test_config_patch_persists_and_reloads(tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    app = create_app()
    async with AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as ac:
        r = await ac.patch("/api/v1/config", json={"hitl": {"timeout_seconds": 60}})
        assert r.status_code == 200
        assert r.json()["hitl"]["timeout_seconds"] == 60
    reloaded = load_ade_config(Path(".loopforge/ade.yaml"))
    assert reloaded.hitl.timeout_seconds == 60


@pytest.mark.asyncio
async def test_config_patch_invalid_does_not_write(tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    (tmp_path / ".loopforge").mkdir(exist_ok=True)
    before = (tmp_path / ".loopforge" / "ade.yaml").read_text() if (tmp_path / ".loopforge" / "ade.yaml").exists() else None
    app = create_app()
    async with AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as ac:
        r = await ac.patch("/api/v1/config", json={"hitl": {"timeout_seconds": "abc"}})
        assert r.status_code == 422
```

`agentes/LoopForge/tests/test_hitl_timeout.py`:
```python
import asyncio
import pytest
from lf.orchestrator.task_dispatcher import TaskDispatcher


@pytest.mark.asyncio
async def test_hitl_expiry_broadcasts_decision_expired(monkeypatch):
    events = []
    monkeypatch.setattr("lf.orchestrator.task_dispatcher._broadcast_ws", lambda event, *a, **k: events.append(event))
    dispatcher = TaskDispatcher(mock_llm=True, interactive=True, hitl_timeout_seconds=0)
    # hitl_timeout_seconds=0 -> expira imediatamente na 1a pausa
    with pytest.raises(SystemExit):  # se o timeout default aborta; ajustar ao comportamento final
        await asyncio.to_thread(dispatcher.dispatch, _mini_task())
    assert "human_decision_expired" in events
```

> Nota: se `hitl_timeout_seconds=0` abortar de vez (comportamento atual "x"), o teste será ajustado para verificar `decision_expired` no caminho de expiração em vez de SystemExit — o contrato final é: run transita para `decision_expired`, evento WS `human_decision_expired` é broadcastado, decisão tardia via `/decide` continua aceita, abort/resume seguem funcionando.

- [ ] **Step 2: Run test to verify it fails**

Run (em `agentes/LoopForge`): `pytest tests/test_config_api.py tests/test_hitl_timeout.py -v`
Expected: FAIL com `ModuleNotFoundError: lf.api.config` e `human_decision_expired` ausente.

- [ ] **Step 3: Write minimal implementation**

`src/lf/api/config.py`:
```python
"""GET/PATCH /api/v1/config — leitura/escrita do .loopforge/ade.yaml."""
from pathlib import Path

from fastapi import APIRouter, HTTPException
from pydantic import ValidationError

from lf.config.loader import load_ade_config, save_ade_config
from lf.config.schema import AdeConfig

config_router = APIRouter(prefix="/api/v1/config", tags=["Config"])

ADE_YAML = Path(".loopforge/ade.yaml").resolve()


@config_router.get("")
async def get_config() -> AdeConfig:
    return load_ade_config(ADE_YAML)


@config_router.patch("")
async def patch_config(payload: dict):
    current = load_ade_config(ADE_YAML)
    merged = current.model_copy(deep=True)
    for key, value in payload.items():
        if hasattr(merged, key):
            setattr(merged, key, merged.__class__.__annotations__[key](**value) if isinstance(value, dict) else value)
    try:
        validated = AdeConfig(**merged.model_dump())
    except ValidationError as exc:
        raise HTTPException(status_code=422, detail=exc.errors()) from exc
    save_ade_config(validated, ADE_YAML)
    return validated
```

Em `src/lf/api/app.py`:
```python
from lf.api.config import config_router
app.include_router(config_router)
```

Em `src/lf/orchestrator/task_dispatcher.py`:
- No `__init__`, carregar o timeout do `ade.yaml` quando não for explicitamente passado:
```python
def __init__(self, ..., hitl_timeout_seconds: int | None = None):
    if hitl_timeout_seconds is None:
        from lf.config.loader import load_ade_config
        hitl_timeout_seconds = load_ade_config().hitl.timeout_seconds
    self.hitl_timeout_seconds = hitl_timeout_seconds
```
- No caminho de expiração do HITL (`_human_interrupt_handler` / `_check_remote_decision`), substituir o abort silencioso por:
```python
# timeout expirado sem decisão
self._broadcast_ws("human_decision_expired", run_id, {"node": next_node, "timeout_seconds": self.hitl_timeout_seconds})
run_status = "decision_expired"   # transição explícita (E10); late /decide continua aceito
```
- Garantir que decisão tardia via `/decide` continua sendo registrada (a tabela `human_decisions` não muda) e abort/resume permanecem.

- [ ] **Step 4: Run test to verify it passes**

Run (em `agentes/LoopForge`):
```
pytest tests/test_config_api.py tests/test_hitl_timeout.py tests/test_resume_checkpoint.py tests/test_hitl_ux_enhancements.py -v
```
Expected: PASS (config GET/PATCH com validação e persistência; `decision_expired` + `human_decision_expired` no timeout; HITL existente segue verde).

- [ ] **Step 5: Commit**

```bash
git add tests/test_config_api.py tests/test_hitl_timeout.py src/lf/api/config.py src/lf/api/app.py src/lf/orchestrator/task_dispatcher.py
git commit -m "feat: config API + HITL timeout gracioso decision_expired (E9/F1-13)"
```

---

### Task 11: Verificação final da Fase 1

**Files:**
- Nenhum código novo — rodar suíte completa.

- [ ] **Step 1: Run full test suite + coverage**

Run (em `agentes/LoopForge`):
```
pytest tests/ --cov=src/lf --cov-fail-under=75
```
Expected: PASS — 217 testes existentes + novos da Fase 1; cobertura >= 75%.

- [ ] **Step 2: Smoke CLI + rotas novas**

Run (em `agentes/LoopForge`):
```
lf run --mock --idea "validacao fase 1" && lf serve --no-ui --port 8123 & sleep 2 && curl -s http://127.0.0.1:8123/health && curl -s http://127.0.0.1:8123/api/v1/trajectories/proj-task-1/export
```
Expected: health `{"status":"ok"}` (ou equivalente) e envelope JSON `schema_version: "1.0"`; dashboard não acessível (`/dashboard` 404).

- [ ] **Step 3: Commit quaisquer ajustes**

```bash
git add -A
git commit -m "feat: fase 1 ADE — checkpointer async, trajectories, MCP, native/ollama providers, --no-ui, ade.yaml"
```

---

## Self-Review

**1. Cobertura do spec (task por task):**
- F1-1 AsyncSqliteSaver + WAL → Tasks 2-3 (factory + migração do dispatcher) e Task 11 (smoke).
- F1-2 rotas trajectories (checkpoints/export/import/fork) → Task 4.
- F1-3 envelope versionado `{schema_version, thread_id, created_at, steps, events}` → Task 4 (`TrajectoryEnvelope`).
- F1-4 sem prune → Global Constraints (nenhuma task de prune).
- F1-5 SDK oficial mcp → Task 5 (`uv add mcp`).
- F1-6 bridge deny-by-default + MCPPermissionDenied → Task 5 (permissions.py/registry.py).
- F1-7 NativeLLMProvider primário + cadeia nativo→OpenCode→Mock → Task 7.
- F1-8 `stream(messages, model, ...) -> AsyncIterator[str]` → Task 7.
- F1-9 OllamaProvider + /api/tags → Task 8.
- F1-10 `lf serve --no-ui` → Task 9.
- F1-11 ade.yaml schema + GET/PATCH /api/v1/config → Tasks 1 e 10.
- F1-12 run é a unidade; fork deriva run → Task 4 (`/fork` retorna `fork_thread_id`).
- F1-13 HITL timeout gracioso (`decision_expired` + `human_decision_expired`) → Task 10.
- Seção 4.1 WAL explícito antes do setup → Task 2 (teste `_wal_mode == "wal"`).
- Seção 4.4 cache só payload final → Task 7 (`llm_cache.set(full_prompt, text)` só no final).
- Seção 7 testes por item → Tasks 2-4 (1a), 5-6 (1b), 7 (1d), 8 (1c), 10 (E9).

**2. Placeholder scan:** nenhum "TBD"/"TODO"; todos os steps têm código concreto; `_dispatch_async`/`_resume_async`/`_list_checkpoints_async` são métodos novos referenciados apenas dentro de Task 3 (definidos no mesmo step). Ajustes de nomes reais de constantes (`DEFAULT_OPENROUTER_BASE_URL`, base URL de `call_openrouter_api`) são anotados como notas em vez de TBD — conferir ao implementar.

**3. Consistência de tipos:** `AdeConfig` (Task 1) é consumido por Tasks 5, 6, 7, 8, 10 com os mesmos nomes de campos (`mcp_servers`, `providers.ollama_base_url`, `hitl.timeout_seconds`); `create_async_checkpointer` (Task 2) usado em Tasks 3-4; `MCPRegistry(config)`/`start_all`/`stop_all`/`list_tools`/`call_tool` consistentes entre Tasks 5-6; `NativeLLMProvider.stream() -> AsyncIterator[str]` consistente em Task 7 e nos testes de Task 8; `create_app(ui_enabled=...)` consistente entre Tasks 9-11.

---

*Próximo passo após aprovação deste plano: escolher o modo de execução (subagent-driven vs inline) e executar task por task.*
